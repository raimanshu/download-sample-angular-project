import { SampleSharedPipePipe } from './sample-shared-pipe.pipe';

describe('SampleSharedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SampleSharedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
